# Airplane-war-game
## Prerequisite

The program uses the `pygame` library which you can install by the following command.

```shell
pip3 install pygame
```



## Usage

To run the script please execute the following command.

```shell
python3 plane_main.py
```

